// Arquivo enclave.edl
enclave{
    trusted{
        public long enclave_sum(
            [in, size=array_size] int *array,
            size_t array_size
        );
    };
    untrusted{}; // Nenhuma função a declarar aqui.
};